import 'package:meta/meta.dart';

/// Temp dev flag
///
/// Can be use as a todo for weird code. `if (devTrue)...;`
@doNotSubmit
const devTrue = true;

/// Temp dev flag
///
/// Can be use as a todo for weird code. `if (devFalse)...; // skipping`
@doNotSubmit
const devFalse = false;
